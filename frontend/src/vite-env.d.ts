/// <reference types="vite/client" />

// Injected by `define` in vite.config.ts at build time. Kept here (instead of
// inline in each consumer) so the symbol is globally typed.
declare const __APP_VERSION__: string;
