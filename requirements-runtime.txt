# Runtime dependencies for `pip download` in scripts/pack.sh — kept in sync
# with pyproject.toml's [project] dependencies + [project.optional-dependencies].runtime.
# Listing them in a flat file is more reliable than letting `pip download`
# resolve the wheel/sdist directly off pyproject.toml.

fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
pydantic-settings>=2.6
httpx>=0.27
notebooklm-py==0.4.1
